import React from 'react'
import PropTypes from 'prop-types'

const Cell = ({type}) => {
    return (
        <div>
            Cell
        </div>
    )
}

Cell.propTypes = {

}

export default Cell
